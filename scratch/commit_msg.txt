fix: overhaul attendance timezone handling and punch processing

- Centralize IST time handling (src/lib/time.js): storage stays UTC,
  @db.Date columns are IST-day at UTC midnight, display converts via Asia/Kolkata.
  Server timezone no longer affects correctness.
- Rewrite iClock punch processing (src/lib/punchProcessor.js): idempotent,
  punch-array-derived inAt/outAt so batch re-uploads/retries can no longer
  clobber punch-out times; OUT punches can no longer spawn spurious
  clocked-in sheets; same-day/open/overnight matching; robust datetime
  parsing. AI-device JSON endpoint now processes punches instead of dropping
  them.
- Fix reports: shiftEndMins ReferenceError, missing total work-hours
  accumulation, date-string shift/leave matching (removes 5.5h boundary bug),
  flexible no-shift handling for rotational staff; remove temporary
  /fix-tz-data endpoint.
- Make dashboard/portal/timesheets/compoff/leaves/workShifts server-TZ
  independent via the time helpers.
- Frontend: format punch times in IST and date-only columns in UTC
  (EmployeePortal, Timesheets, Approvals).
- Add scripts/repair-attendance-data.js (dry-run by default; --apply writes
  after backing up) and refactor scripts/process_pending_logs.js onto the
  shared punch processor.
- Add unit/integration tests under scratch/ (time logic, punch processor
  scenarios, repair script).
